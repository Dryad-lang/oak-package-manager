# 📄 Configuração de Ambiente - Registry Dryad
# Copie este arquivo para .env e ajuste os valores

#──────────────────────────────────────────────────────────
# 🌐 CONFIGURAÇÃO DO DOMÍNIO
#──────────────────────────────────────────────────────────
DOMAIN=oak.dryadlang.org
# Para produção use seu domínio: registry.dryad-lang.org

#──────────────────────────────────────────────────────────
# 🔐 SEGURANÇA - GERE SENHAS SEGURAS!
#──────────────────────────────────────────────────────────

# JWT Secret (gere com: openssl rand -hex 32)
JWT_SECRET=change_this_to_a_secure_random_string_32_chars_minimum

# Gitea Secret Key (gere com: openssl rand -hex 32)
GITEA_SECRET_KEY=another_secure_random_string_for_gitea_configuration

# Senhas dos bancos de dados (use senhas fortes!)
POSTGRES_PASSWORD=secure_gitea_database_password_here
REGISTRY_DB_PASSWORD=secure_registry_database_password_here
REDIS_PASSWORD=secure_redis_password_here

#──────────────────────────────────────────────────────────
# 🗄️ CONFIGURAÇÃO DOS BANCOS DE DADOS
#──────────────────────────────────────────────────────────

# Gitea Database
POSTGRES_USER=gitea
POSTGRES_DB=gitea

# Registry Database
REGISTRY_DB_USER=registry
REGISTRY_DB_NAME=dryad_registry
REGISTRY_DB_HOST=postgres-registry
REGISTRY_DB_PORT=5432

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

#──────────────────────────────────────────────────────────
# 🚀 CONFIGURAÇÃO DOS SERVIÇOS
#──────────────────────────────────────────────────────────

# Portas dos serviços (altere se necessário)
GITEA_HTTP_PORT=3000
GITEA_SSH_PORT=2222
REGISTRY_API_PORT=4000
WEB_INTERFACE_PORT=3001
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# Node.js Environment
NODE_ENV=production

#──────────────────────────────────────────────────────────
# 📦 CONFIGURAÇÃO DE UPLOADS
#──────────────────────────────────────────────────────────

# Limite de tamanho dos pacotes (em bytes)
MAX_PACKAGE_SIZE=104857600  # 100MB
UPLOAD_TIMEOUT=300          # 5 minutos

# Diretórios de storage
UPLOAD_DIR=/app/uploads
PACKAGES_DIR=/app/packages
CACHE_DIR=/app/cache

#──────────────────────────────────────────────────────────
# 🔗 URLs DOS SERVIÇOS
#──────────────────────────────────────────────────────────

# URLs para desenvolvimento
NEXT_PUBLIC_API_URL=http://oak.dryadlang.org:4000
NEXT_PUBLIC_GITEA_URL=http://oak.dryadlang.org:3000

# Para produção, use:
# NEXT_PUBLIC_API_URL=https://registry.dryad-lang.org
# NEXT_PUBLIC_GITEA_URL=https://registry.dryad-lang.org/git

#──────────────────────────────────────────────────────────
# 📧 CONFIGURAÇÃO DE EMAIL (OPCIONAL)
#──────────────────────────────────────────────────────────

# Configuração SMTP para notificações
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=registry@dryad-lang.org

#──────────────────────────────────────────────────────────
# 🔍 CONFIGURAÇÃO DE LOGGING
#──────────────────────────────────────────────────────────

# Nível de log (error, warn, info, debug)
LOG_LEVEL=info

# Diretório de logs
LOG_DIR=/app/logs

#──────────────────────────────────────────────────────────
# 🎛️ CONFIGURAÇÕES AVANÇADAS
#──────────────────────────────────────────────────────────

# Rate limiting (requests por segundo)
API_RATE_LIMIT=10
UPLOAD_RATE_LIMIT=2

# Configuração de CORS
ALLOWED_ORIGINS=http://oak.dryadlang.org:3001,https://dryad-lang.org

# Session timeout (em segundos)
SESSION_TIMEOUT=604800  # 7 dias

# Configuração do Gitea
GITEA_APP_NAME=Dryad Package Repository
GITEA_ADMIN_USER=admin
GITEA_ADMIN_EMAIL=admin@dryad-lang.org

#──────────────────────────────────────────────────────────
# 🔒 SSL/TLS (PRODUÇÃO)
#──────────────────────────────────────────────────────────

# Configuração SSL para produção
SSL_CERT_PATH=/etc/nginx/ssl/fullchain.pem
SSL_KEY_PATH=/etc/nginx/ssl/privkey.pem
SSL_EMAIL=admin@dryad-lang.org

# Força HTTPS (true/false)
FORCE_HTTPS=false

#──────────────────────────────────────────────────────────
# 🧪 CONFIGURAÇÕES DE DESENVOLVIMENTO
#──────────────────────────────────────────────────────────

# Apenas para desenvolvimento - não use em produção!
DEBUG_MODE=false
ENABLE_DEBUG_LOGS=false

# Configurações de teste
TEST_DATABASE_URL=postgresql://test:test@oak.dryadlang.org:5433/dryad_test

#──────────────────────────────────────────────────────────
# 📊 MONITORAMENTO (OPCIONAL)
#──────────────────────────────────────────────────────────

# Configuração do monitoramento
ENABLE_METRICS=true
METRICS_PORT=9090

# Alertas (Webhook URLs)
ALERT_WEBHOOK_URL=https://hooks.slack.com/your-webhook-url
MONITORING_EMAIL=alerts@dryad-lang.org

#──────────────────────────────────────────────────────────
# 💾 BACKUP E STORAGE
#──────────────────────────────────────────────────────────

# Configuração de backup automático
BACKUP_ENABLED=true
BACKUP_SCHEDULE="0 2 * * *"  # Todo dia às 2h
BACKUP_RETENTION_DAYS=30

# Storage externo (S3-compatible)
# S3_BUCKET=dryad-packages
# S3_REGION=us-east-1
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key

#──────────────────────────────────────────────────────────
# 🏷️ INFORMAÇÕES DO REGISTRY
#──────────────────────────────────────────────────────────

REGISTRY_NAME=Dryad Package Registry
REGISTRY_DESCRIPTION=Official package registry for the Dryad programming language
REGISTRY_CONTACT=registry@dryad-lang.org
REGISTRY_TERMS_URL=https://dryad-lang.org/terms
REGISTRY_PRIVACY_URL=https://dryad-lang.org/privacy

#══════════════════════════════════════════════════════════
# 📝 NOTAS IMPORTANTES:
#
# 1. 🔐 SEMPRE mude as senhas padrão antes de usar em produção
# 2. 🔑 Gere JWT_SECRET e GITEA_SECRET_KEY únicos e seguros
# 3. 🌐 Configure o DOMAIN correto para seu servidor
# 4. 📧 Configure SMTP para notificações de email
# 5. 🔒 Ative SSL/HTTPS em produção
# 6. 💾 Configure backups automáticos
# 7. 📊 Ative monitoramento em produção
#
# Para gerar senhas seguras:
# openssl rand -hex 32
# ou use: https://www.uuidgenerator.net/
#══════════════════════════════════════════════════════════